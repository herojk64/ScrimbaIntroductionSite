import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Creating root folder into const so the value wont change
const root = document.getElementById("root");

ReactDOM.render(<App />, root);